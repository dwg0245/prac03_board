
group = 'org.example'
version = '0.0.1-SNAPSHOT'
description = 'board_command'

dependencies {
    // 내가 만든 코드 가져올 때, 의존성 추가
    implementation project(':board_core')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

